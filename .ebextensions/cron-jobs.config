files:
  "/etc/cron.d/wp-cron":
    mode: "000644"
    owner: root
    group: root
    content: |
      * * * * * root /usr/local/bin/wp --allow-root --path=/var/app/current cron event run --due-now >> /var/log/wp-cron.log

commands:
  remove_old_cron:
    command: "rm -f /etc/cron.d/wp-cron.bak"